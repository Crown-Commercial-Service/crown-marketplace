<%= f.fields_for :authorised_contact_detail do |ff|%>
  <%= render partial: "#{partial_prefix}/new_contact_details_address",
              locals: {f: f, ff: ff, page_contact: 'authorised_contact_detail'} %>
<% end %>
