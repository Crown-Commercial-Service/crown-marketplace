<% if params[:delete] == 'y' && procurement_can_be_deleted? %>
  <%= render 'delete_procurement' %>
<% else %>
  <%= render @view_name %>
<% end %>
<% content_for(:page_title, @page_description.heading_details.text) unless @page_description.nil?  %>